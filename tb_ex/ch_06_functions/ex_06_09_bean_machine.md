# ex_06_09_bean_machine

See the textbook for the exercise text.

## Assessment criteria

| Criterion | Description | Weight (%) |
|---|---|---:|
| Ball simulation | Implements drop_ball(num_rows) to simulate equally likely left or right bounces through the requested rows and return a final slot from 0 to num_rows. | 40 |
| Slot counts | Implements run_simulation(num_balls, num_rows) using drop_ball and returns a list of num_rows + 1 slot counts accounting for every requested ball. | 30 |
| Histogram | Implements display_histogram(counts) to show each slot and its count with an asterisk bar representing that count. | 20 |
| Main program | Runs the simulation with some balls and some rows and passes the returned counts to the histogram function. | 10 |
| **Total** | | **100%** |
